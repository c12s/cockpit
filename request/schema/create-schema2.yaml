name: schema
organization: c12s
namespace: default
type: object
properties:
  db_config:
    type: object
    properties:
      address:
        type: string
    required:
      - address
required:
  - db_config
