constellations:
  version: v1
  kind: Namespaces # May be [Namespaces]
  metadata:
    namespace: default
    taskName: default
    queue: default
    forceNamespaceQueue: true
  payload: # May contains [labels and namespace with name]
    namespace:
      name: my_namespace
    labels:
      l1: v1
      l2: v2
